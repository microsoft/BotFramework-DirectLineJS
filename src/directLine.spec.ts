import { DirectLine } from '../lib/node/directLine';

describe('The DirectLine class', () => {
  let directLine: DirectLine;

  beforeAll(() => {

  });

  it('should initiate a new conversation', () => {

  });
});
